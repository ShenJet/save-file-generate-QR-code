{
  "name": "save-file-generate-QR-code",
  "version": "1.0.0",
  "description": "save files to server and generate QR-code for them",
  "main": "app.js",
  "repository": "git@github.com:ShenJet/save-file-generate-QR-code.git",
  "author": "ShenJet <985184097@qq.com>",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist/* && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "yarn build && nodemon dist/app.js"
  },
  "dependencies": {
    "koa": "^2.7.0",
    "koa-body": "^4.1.0",
    "koa-static": "^5.0.0",
    "qrcode": "^1.3.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.3"
  }
}
